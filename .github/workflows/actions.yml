name: checks
on:
  push:
    branches:
      - main
  pull_request:
jobs:
  gofmt:
    name: go fmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
      - uses: Jerome1337/gofmt-action@v1.0.5

  govet:
    name: go vet
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
      - run: |
          go vet ./...

  golangci:
    name: golangci-lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: v2.13.1
          only-new-issues: true
          args: --enable bodyclose,forcetypeassert,misspell --timeout=5m
      - name: golangci-lint-fmt
        run: golangci-lint fmt --enable gci --diff
